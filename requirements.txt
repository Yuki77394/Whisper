# ── WhisperX – Python dependencies ──────────────────────────────
# Telegram framework (Pyrogram v2 – async, fast, MTProto-based)
pyrogram==2.0.106
TgCrypto==1.2.5

# Async MongoDB driver
# Pin pymongo EXACTLY — motor 3.3.2 breaks with pymongo >=4.8
# (ImportError: cannot import name '_QUERY_OPTIONS' from 'pymongo.cursor')
motor==3.3.2
pymongo==4.6.3

# Config / env handling
python-dotenv==1.0.1

# Misc utilities
python-dateutil==2.9.0.post0

# Webhook mode (optional, only needed if you switch from polling)
aiohttp==3.9.5
